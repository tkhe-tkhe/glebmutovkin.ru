// Раздел «Фото». Порядок в списке = порядок на странице.
// Добавить снимок: положить файл в src/images/foto/ и дописать строку
// в foto.json (или добавить карточку в админке).
// Подписей у снимков нет — решение Глеба.
//
// С 18 августа 2026 список лежит в foto.json — это формат, который читает
// админка. Здесь остался тип и пояснения: JSON комментариев не держит.

import dannye from './foto.json';

export interface Foto {
  file: string; // путь целиком: /src/images/foto/имя.jpg — так его пишет админка
  alt: string;  // для читалок и на случай, если картинка не загрузилась
}

export const foto: Foto[] = dannye.foto;
