// Настройки сайта целиком. Меняются здесь, а не по десяти файлам.
//
// С 18 августа 2026 значения лежат в sait.json — это формат, который читает
// админка. Здесь остались пояснения: JSON комментариев не держит.

import dannye from './sait.json';

// Пускать ли поисковики.
//
// zakryt = true  — на каждой странице стоит noindex, в robots.txt «Disallow: /»,
//                  карта сайта не отдаётся. Сайт открыт людям по адресу,
//                  но в выдаче не появится. Так живём первые дни после запуска.
// zakryt = false — обычное поведение: индексируется всё, кроме раздела «Гайды»
//                  (у него свой флаг в src/data/gaidy.json) и служебных страниц.
//
// Открывать сайт — одна правка здесь плюс добавление карты сайта
// в Яндекс.Вебмастер и Google Search Console.
export const zakryt: boolean = dannye.zakryt;

// Канонический адрес. Без www: www.glebmutovkin.ru редиректом уходит сюда
// (правило в public/.htaccess), чтобы у страницы был один адрес, а не два.
export const adres: string = dannye.adres;

// Домен tkhe-tkhe.ru куплен раньше и остаётся у Глеба неиспользованным:
// сайт живёт на имени, а не на названии группы. Если однажды понадобится —
// его можно направить сюда редиректом или отдать под что-то другое.
