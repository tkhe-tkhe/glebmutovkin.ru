// Текст страницы «Обо мне» — слова Глеба, из формы o-mne-forma.txt
// (правка от 16 августа 2026). Держим отдельно от вёрстки, чтобы правка
// текста не задевала разметку.
//
// При переносе поправлено только механическое: опечатка «назвние»,
// пробел перед точкой, «все» → «всё», запятая в «делаю всё, что могу»
// и многоточие одним знаком. Слова и порядок — его.
//
// С 18 августа 2026 сами абзацы лежат в o-mne.json — это формат, который
// читает админка. Здесь остался только тип и пояснения: JSON комментариев
// не держит, а страницам по-прежнему нужен один и тот же импорт.

import dannye from './o-mne.json';

export const abzacy: string[] = dannye.abzacy;
